Docker folder: place Dockerfiles and docker-compose configurations here.

Suggested:
- dockerfiles for storefront, payload, worker
- docker-compose.dev.yml to run storefront + payload + mongo + redis locally
